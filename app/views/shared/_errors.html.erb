<% if obj.errors.any? %>
  <div id="error_expl" class="message is-danger">
    <div class="message-header">
      <h3><%= pluralize(obj.errors.count, "error") %> prohibited this Member from being saved:</h3>
    </div>
    <div class="message-body">
      <ul>
        <% obj.errors.full_messages.each do |msg| %>
        <a href="#" class="close" data-dismiss="danger">
          &#215;
        </a>
        <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>
